# Airtable Configuration
AIRTABLE_API_KEY=your_airtable_api_key_here
AIRTABLE_BASE_ID=your_airtable_base_id_here
AIRTABLE_TABLE_NAME=TTB COLAs

# TTB Scraper Configuration
TTB_PRODUCT_NAME=Shottys
TTB_VENDOR_CODE=23153
TTB_DELAY=1.0

# Sync Strategy Configuration
# Options: incremental (default), full, replace
# - incremental: Only create new records (fastest, recommended)
# - full: Update existing records and create new ones (slower)
# - replace: Delete all and recreate (DESTRUCTIVE, requires CONFIRM_REPLACE=true)
SYNC_STRATEGY=incremental

# Safety flag for REPLACE strategy (required if using replace)
# CONFIRM_REPLACE=true
